# Contributing

Requirements: Node.js 20 or newer and pnpm 9.

```bash
pnpm install
pnpm run ci
```

Keep pull requests focused. Add or update the smallest relevant test when behavior changes. If a published package changes, run `pnpm changeset` and describe the user-visible impact.
